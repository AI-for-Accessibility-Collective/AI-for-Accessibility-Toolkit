node_modules/
.DS_Store
*.log
.env
content.bundle.js.map
cli-tools.bundle.js.map

# Python
__pycache__/
*.pyc
*.egg-info/
dist/
build/
*.egg
.claude/worktrees/

# The analysis, copied in from the research repository by its install script.
# Mechanism lives here; the corpus text does not.
personalized-extension/extension/validation/corpus.json

# The task model the reasoner reads a page against. Same rule as the corpus:
# it is a gold built from the recorded studies and belongs in the research
# repository, which copies it here. Without it the layer runs the Amazon path.
personalized-extension/extension/validation/taskmodel.json
